🧪 Task: Simple Student Score Analyzer (C)
Requirements

Write a C program that:

Asks the user for the number of students (max 50)

Stores each student’s score (0–100)

Calculates and prints:

Average score

Highest score

Lowest score

Prints how many students scored above the average

Constraints

Use arrays (not linked lists)

Do not use global variables

Validate user input where reasonable

Avoid unsafe functions (gets, unchecked scanf, etc.)
